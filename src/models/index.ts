import UserModel from './user.model';
import ArticleModel from './article.model';
import ArticleMediaModel from './article-media.model';

const Models = {
  UserModel,
  ArticleModel,
  ArticleMediaModel,
};

export default Models;
